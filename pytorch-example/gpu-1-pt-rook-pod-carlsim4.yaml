apiVersion: v1
kind: Pod
metadata:
  name: gpu-one-rook-carlsim4
spec:
  containers:
  - name: gpu-container
    image: hkashyap/evolverl
    imagePullPolicy: Always
    args: ["sleep", "36500000"]
    resources:
      limits:
        nvidia.com/gpu: 4
        memory: 16Gi
      requests:
        nvidia.com/gpu: 4
        memory: 16Gi
    volumeMounts:
      - mountPath: /dev/shm
        name: mem
  restartPolicy: Never
  volumes:
    - name: mem
      emptyDir:
        medium: Memory
