apiVersion: v1
kind: Pod
metadata:
  name: gpu-one-rook-hk-vi
spec:
  containers:
  - name: gpu-container
    image: hkashyap/evolverl
    imagePullPolicy: Always
    args: ["sleep", "36500000"]
    resources:
      limits:
        nvidia.com/gpu: 1
        memory: 16Gi
      requests:
        nvidia.com/gpu: 1
        memory: 16Gi
    volumeMounts:
      - mountPath: /pervol-vi
        name: pervol-vi
      - mountPath: /dev/shm
        name: mem
  restartPolicy: Never
  volumes:
    - name: pervol-vi
      persistentVolumeClaim:
        claimName: pervol-vi
    - name: mem
      emptyDir:
        medium: Memory
