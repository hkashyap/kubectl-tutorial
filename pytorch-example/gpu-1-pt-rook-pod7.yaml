apiVersion: v1
kind: Pod
metadata:
  name: gpu-one-rook-hk-vii
spec:
  containers:
  - name: gpu-container
    image: hkashyap/evolverl
    imagePullPolicy: Always
    args: ["sleep", "36500000"]
    resources:
      limits:
        nvidia.com/gpu: 1
        memory: 16Gi
      requests:
        nvidia.com/gpu: 1
        memory: 16Gi
    volumeMounts:
      - mountPath: /pervol-vii
        name: pervol-vii
      - mountPath: /dev/shm
        name: mem
  restartPolicy: Never
  volumes:
    - name: pervol-vii
      persistentVolumeClaim:
        claimName: pervol-vii
    - name: mem
      emptyDir:
        medium: Memory
