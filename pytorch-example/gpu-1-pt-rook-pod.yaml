apiVersion: v1
kind: Pod
metadata:
  name: gpu-one-rook-hk
spec:
  containers:
  - name: gpu-container
    image: hkashyap/evolverl
    imagePullPolicy: Always
    args: ["sleep", "36500000"]
    resources:
      limits:
        nvidia.com/gpu: 1
        memory: 8Gi
      requests:
        nvidia.com/gpu: 1
        memory: 8Gi
    volumeMounts:
      - mountPath: /pervol
        name: pervol
      - mountPath: /dev/shm
        name: mem
  restartPolicy: Never
  volumes:
    - name: pervol
      persistentVolumeClaim:
        claimName: pervol
    - name: mem
      emptyDir:
        medium: Memory
